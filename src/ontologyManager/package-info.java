/**
 * The ontology Manager Package, is a package that serves as a convenience wrapper to the OWLAPI,
 * for retrieving required information from the Ontology.
 * The package uses annotation Properties to read various annotations to an OWL Class.
 * The Annotation properties are set by default to read from the OBI and OBI-WS Ontologies.
 * Make required changes to ontologySimilarities properties file when using with Other Ontologies
 * 
 */
package ontologyManager;
